@* 
    This file defines the default layout for all Razor views in this folder (and subfolders).
    By setting the `Layout` property here, all views will automatically use the specified layout unless they override it.
*@

@{
    @* Setting the default layout for the views to _Layout.cshtml *@
    Layout = "_Layout";
}
